@Library('todo') _

todo (
        COMPONENT             : 'login',
        PROJECT_NAME          : "TODO",
        SLAVE_LABEL           : "master",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "GOLANG"
)
