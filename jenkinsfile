@Library('todo') _

roboshop (
        COMPONENT             : 'login',
        PROJECT_NAME          : "TODO",
        SLAVE_LABEL           : "GOLANG",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "GOLANG"
)
